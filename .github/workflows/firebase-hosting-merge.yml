# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set Environment and Build
        run:  | 
          echo "export const environment = {" > src/environments/environment.ts
          echo "  production: true," >> src/environments/environment.ts
          echo "  firebase: {" >> src/environments/environment.ts
          echo "  apiKey : '${{ secrets.NG_APP_FIREBASE_API_KEY }}'," >> src/environments/environment.ts
          echo "  authDomain : '${{ secrets.NG_APP_FIREBASE_AUTH_DOMAIN }}'," >> src/environments/environment.ts
          echo "  projectId : '${{ secrets.NG_APP_FIREBASE_PROJECT_ID }}'," >> src/environments/environment.ts
          echo "  storageBucket : '${{ secrets.NG_APP_FIREBASE_STORAGE_BUCKET }}'," >> src/environments/environment.ts
          echo "  messagingSenderId : '${{ secrets.NG_APP_FIREBASE_APIKEY_MESSAGING_SENDER_ID }}'," >> src/environments/environment.ts
          echo "  appId : '${{ secrets.NG_APP_FIREBASE_APP_ID }}'," >> src/environments/environment.ts
          echo " }" >> src/environments/environment.ts
          echo "};" >> src/environments/environment.ts
          npm ci && npm run build
        env:
          CI: false
      
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SELFSERVICEDESK_BCD33 }}
          channelId: live
          projectId: selfservicedesk-bcd33
